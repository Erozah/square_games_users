meta {
  name: 3. Valider Alice (Users API)
  type: http
  seq: 3
}

get {
  url: {{usersBaseUrl}}/users/{{aliceId}}/valid
  body: none
  auth: none
}

tests {
  test("Status 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.equal(true);
  });
}
