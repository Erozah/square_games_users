meta {
  name: 7. Coup par le joueur courant -> 200 OK
  type: http
  seq: 7
}

post {
  url: {{gamesBaseUrl}}/games/{{gameId}}/moves
  body: json
  auth: none
}

headers {
  Authorization: Bearer<token>: {{currentPlayerId}}
}

body:json {
  {
    "x": 0,
    "y": 0
  }
}

tests {
  test("Status 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
